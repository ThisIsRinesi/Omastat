{
  "schemaVersion": 1,
  "id": "local.omastat",
  "name": "Omastat",
  "version": "0.1.0",
  "author": "rinesi",
  "license": "MIT",
  "description": "Native Omarchy Quattro bar widget and popup for local Omastat usage data.",
  "kinds": ["bar-widget"],
  "activation": "on-demand",
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Omastat",
    "description": "Shows today's top focused app, focused/open totals, and a popup with the leading apps.",
    "category": "Utilities",
    "aliases": ["omastat", "usage", "time"],
    "allowMultiple": false,
    "defaults": {
      "refreshIntervalSec": 60,
      "command": "omastat --json today",
      "terminalCommand": "xdg-terminal-exec --hold bash -lc 'PATH=\"$HOME/.cargo/bin:$HOME/.local/bin:$PATH\"; omastat tui'"
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 15,
        "max": 3600,
        "step": 15,
        "defaultValue": 60
      },
      {
        "key": "command",
        "type": "string",
        "label": "Report command",
        "defaultValue": "omastat --json today"
      },
      {
        "key": "terminalCommand",
        "type": "string",
        "label": "Terminal command",
        "defaultValue": "xdg-terminal-exec --hold bash -lc 'PATH=\"$HOME/.cargo/bin:$HOME/.local/bin:$PATH\"; omastat tui'"
      }
    ]
  }
}
